<UserControl x:Class="Paratoner.UserControls.AdminOperations"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="350">
    <GroupBox Header="ADMIN OPERATIONS">
        <StackPanel>
            <ComboBox FontSize="26" IsEditable="True" Controls:TextBoxHelper.Watermark="CHOOSE A HOME" Margin="5" />
            <DataGrid Height="200" Margin="5" />
            <GroupBox Header="SHOW ALL">
                <Controls:ToggleSwitch IsChecked="True" Margin="5" OnLabel="ON" OffLabel="OFF" />
            </GroupBox>
            <Button Controls:ButtonHelper.CornerRadius="0" Background="Green" Margin="5" FontSize="20">
                <TextBlock>
                    ACCEPT SELECTED MEMBERS
                </TextBlock>
            </Button>
            <Button Controls:ButtonHelper.CornerRadius="0" Background="Red" Margin="5" FontSize="20">
                <TextBlock>
                    DELETE SELECTED MEMBERS
                </TextBlock>
            </Button>
        </StackPanel>
    </GroupBox>
</UserControl>